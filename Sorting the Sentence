#include <iostream>
#include <vector>
using namespace std;

string sort_sentence(string s) {
    vector<string> ans(10);  // stores words at index = position
    string temp;
    int count = 0;

    for (int i = 0; i < s.size(); i++) {
        if (s[i] != ' ') {
            temp += s[i];
        } else {
            int pos = temp.back() - '0'; // get the digit at the end
            temp.pop_back();             // remove that digit
            ans[pos] = temp;             // store the word
            temp.clear();
            count++;
        }
    }

    // process the last word (not followed by a space)
    if (!temp.empty()) {
        int pos = temp.back() - '0';
        temp.pop_back();
        ans[pos] = temp;
        count++;
    }

    string result;
    for (int i = 1; i <= count; i++) {
        result += ans[i] + " ";
    }

    if (!result.empty()) {
        result.pop_back();  // remove trailing space
    }

    return result;
}

int main() {
    string s;
    getline(cin, s);  // use getline to capture full sentence with spaces

    cout << sort_sentence(s) << endl;  // FIX: use cout to print function output

    return 0;
}
